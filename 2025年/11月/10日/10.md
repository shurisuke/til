# ValueObjectまとめ
**値オブジェクト（Value Object）**とは、ドメインにおける「意味のある値」を、String や Integer のようなプリミティブ型ではなく、独自のクラスとして表現する設計手法である。

このクラスは一意性を持たず、イミュータブル（不変）で、副作用のないふるまいを備え、同じ値を持つならば別インスタンスでも等価と見なされる。

これにより、コードの型安全性、表現力、保守性、可読性を向上させることができる。