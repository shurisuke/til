# 2025-11-1：今日のまとめ
今日はsimple_form_forの理解にすごく苦しんだ。
公式githubめちゃくちゃわかりづらくないか？まるまる翻訳して読んでも、望んだ情報を探し当てられないことがザラにあるから大変。情報を見つける力を高めていきたい。


# simple_form_forでのラジオボタン実装方法
基本の書き方
simple_form_for を用いると、ラジオボタンは as: :radio_buttons オプションで簡潔に出力できる

= f.input :eye_catch_send, as: :radio_buttons,
  collection: Article.eye_catch_sends.keys.map { |key| [Article.human_attribute_name("eye_catch_send.#{key}"), key] },
  include_blank: falsepm

この記述で、enum のキーを人間に読みやすいラベルで表示することができる。


# enumとi18nによる選択肢の日本語化

モデル側のenum定義
class Article < ApplicationRecord
  enum eye_catch_send: { left: 0, center: 1, right: 2 }
end

翻訳ファイルでの設定例（config/locales/ja.yml）
ja:
  activerecord:
    attributes:
      article:
        eye_catch_send:
          left: 左寄せ
          center: 中央寄せ
          right: 右寄せ
このように設定することで、ラジオボタンのラベルが enum のキーではなく、ユーザーにとってわかりやすいかんじで表示される。